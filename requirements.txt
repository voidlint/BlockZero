

# -----------------------------
# PyTorch (must be installed first for flash-attn build)
# -----------------------------
torch>=2.3.0       # install specific CUDA/CPU wheel from PyTorch website if needed

# -----------------------------
# Core
# -----------------------------
pydantic-settings==2.2.1
typer==0.12.3
structlog==24.1.0
prometheus-client==0.20.0
PyYAML==6.0.1
fsspec==2024.6.1

# -----------------------------
# ML stack
# -----------------------------
transformers>=4.56.2
accelerate==1.0.0
datasets==3.0.0
bitsandbytes>=0.45.5; platform_system == "Linux"

# Unsloth moved to requirements-unsloth.txt (install after torch)

# TRL for potential GRPO/RLHF support
trl>=0.11.0

# -----------------------------
# API / server (for validator, optional)
# -----------------------------
fastapi==0.110.1
uvicorn[standard]==0.30.0
requests==2.32.3

# -----------------------------
# Messaging backends (pick one)
# -----------------------------
redis==5.0.6
rq==1.16.2

confluent-kafka==2.5.0

# -----------------------------
# Storage backends
# -----------------------------
boto3==1.34.160        # S3
ipfshttpclient==0.8.0a2  # IPFS

# -----------------------------
# Blockchain (Ethereum-compatible, optional)
# -----------------------------
web3==6.19.0
eth-account==0.10.0

# -----------------------------
# Dev & Testing
# -----------------------------
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
hypothesis==6.112.0

ruff==0.6.3
black==24.8.0
mypy==1.11.1
types-redis==4.6.0.20240819
types-PyYAML==6.0.12.20240808

# -----------------------------
# Others
# -----------------------------
torchdata
wandb
aiofiles
python-multipart
git+https://github.com/learning-at-home/hivemind.git@master
matplotlib
pandas
substrate-interface
bittensor==9.12.2 
plotly